/**
 * @description Entry point for the Configuration domain schemas.
 * Provides access to global system settings validation.
 */
import { configurationSchema } from './Configuration.js';

export {
    configurationSchema
};
